{
    "required": true,
    "package": "${modGroup}.${modid}.mixin",
    "compatibilityLevel": "JAVA_${javaVersion}",
    "refmap": "${modid}.refmap.json",
    "maxShiftBy": 5,
    "mixins": [
${mixins}
    ],
    "client": [
${clientMixins}
    ],
    "minVersion": "0.8",
    "maxShiftBy": 5
}